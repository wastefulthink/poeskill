# クイックスタート（3分・専門用語なし）

> 技術的な知識は不要です。順に進めるだけ——3分でAIからの最初の「反論」を体験できます。

🌐 [简体中文](QUICKSTART.md) · [English](QUICKSTART.en.md) · [한국어](QUICKSTART.ko.md) · [Русский](QUICKSTART.ru.md) · [Español](QUICKSTART.es.md) · [日本語](QUICKSTART.ja.md) · [Français](QUICKSTART.fr.md) · [Deutsch](QUICKSTART.de.md) · [Português](QUICKSTART.pt.md)

---

## 覚える必要があるのはこれだけ

**インストール後、覚える必要があるのはコマンド1つだけ：`/poe`**

あなたが何をしようとしているかを判断し、適切なツールへルーティングします。
**32個のスキル名を覚える必要はありません。**

---

## ステップ1 — インストール（約30秒）

ターミナルで（Windowsの場合はGit BashまたはWSLを使用）：

```bash
curl -fsSL https://raw.githubusercontent.com/wastefulthink/poeskill/main/install.sh | bash
```

または、プロジェクトフォルダをダウンロード済みであれば、その中で以下を実行：

```bash
bash install.sh
```

インストーラーがAIツール（Claude Code、Codex、Cline、
WorkBuddyなど）を自動検出し、確認のうえスキルをインストールします。次のように表示されます：

```
[poeskill] done. Run /poe in your Agent to get started.
```

> **bashがない？** [Windowsのコツ](#windowsのコツ)を参照してください。

## ステップ2 — 最初の一歩を入力する（30秒）

AIツールを開いて、そのまま入力——普通の言葉で、フォーマットは不要：

```
/poe I have an idea: a coffee shop AI queue-ordering mini-app. Should I do it?
```

これだけです。あとはAIが引き受けます。

## ステップ3 — 反論を体験する（2分間）

普通のAIなら「いいアイデアですね。作り方はこうです」と言うでしょう。
poeskillを入れたAIは、まずあなたに反論します：

- 前提に異を唱える：「**行列は本当に彼らの最大のペインポイント
  ですか？検証しましたか？**」
- 証拠を求める：「店のオーナーは本当にこれにお金を払いますか？
  証拠はありますか？」
- 反証可能な結論を出す：「**Xが起きない限りやめるべき**」
  （「実行次第」ではなく）

**これがすべてです。まず反論してくれるから、より良い意思決定ができる。**

---

## インストールできたか確認するには

| 方法 | やり方 |
|---|---|
| とにかく試す | `/poe` と入力——オンボーディング／ルーティングが表示されればインストール成功 |
| フォルダを確認 | スキルディレクトリ（例：`~/.claude/skills/`）に32個の `poe-*` フォルダが存在する |
| インストール出力を確認 | `skills installed → <dir> (32 skills)` と出力されていれば成功 |

## よくある場面を、普通の言葉で

この表は覚えなくて大丈夫——`/poe` が自動でルーティングします。
頭の中の全体像をつかむための参考として：

| やりたいこと | こう言うだけ | 裏側で動くもの |
|---|---|---|
| アイデアがやる価値があるか判断したい | 「これやるべきか分析して」 | `/poe-diagnosis` |
| 主張や記事をファクトチェックしたい | 「この主張を検証して」 | `/poe-verify` |
| 何がしたいかうまく言えない | 「この目標を整理して」 | `/poe-goal` |
| やるべきことは分かっているのに始められない | 「なぜずっと先延ばしするんだろう」 | `/poe-action` |
| バズワードが具体的に何を意味するか分からない | 「この言葉を解体して」 | `/poe-deconstruct` |
| いい冒頭が書けない | 「このフックを改善して」 | `/poe-hook` |
| 後で後悔しそうな決断 | 「この決断を長期追跡して」 | `/poe-decision` |

## Windowsのコツ

1. **Git BashまたはWSLを使って** インストーラーを実行してください。これはbashスクリプトであり、どちらにもbashが付属しています。
2. **bashを使いたくない？** 大丈夫です。誰か（あるいはあなたのAIツール）にインストールを実行してもらってください。その後の利用は `/poe` と入力するだけ——ターミナルは不要です。
3. **どこにインストールされた？** インストーラーが `skills installed → <path>` と出力します。そのパスをメモしておいてください。
4. **インストーラーが止まる・エラーが出る？** エラーを `/poe` に貼り付けて（「インストールで問題が発生しました」）、またはIssueを立ててください。

---

## 次のステップ

- 各ツールが何をするか → [README](README.en.md)
- 効果の証明 → [`benchmark/`](benchmark/)（同じ質問：素の状態 6/25 vs
  poeskill入り 25/25）
- 何か問題があれば → `/poe` に「問題があります」と伝えるか、
  [Issue](https://github.com/wastefulthink/poeskill/issues/new)を立ててください

> v3.5.1 ｜ MIT ｜ ローカルで動作：テレメトリなし、サインアップなし、アカウントなし
